const { QuadraticEquation } = require('./quadeq')
const { PolyRegression } = require('./polyreg')
const { Stats } = require('./stats')

module.exports = {
    QuadraticEquation,
    PolyRegression,
    Stats
}